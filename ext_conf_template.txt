# type=string; Allow sending mails only to this address(es). Must be a regular expression string. Comma separated list
whiteListRecipients =
# type=string; Send all mails to this address(es). Comma separated list
recipients =
# type=string; Global sender
sender =
